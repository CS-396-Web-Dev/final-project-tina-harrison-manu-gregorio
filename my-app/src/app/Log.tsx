import { jersey20 } from "./fonts/fonts";

export default function Log() {
    return (
        <div className="bg-black h-2/5 mt-9 rounded-md text-left">
            <h2 className={`${jersey20.className} text-2xl text-white ml-3 mt-3`}>Log</h2>
        </div>
    );
}