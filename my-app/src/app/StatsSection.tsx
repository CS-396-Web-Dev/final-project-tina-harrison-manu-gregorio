import { jersey20 } from "./fonts/fonts";

export default function StatsSection() {
    return (
        <div className="h-1/5 text-left border border-black rounded-md">
            <h2 className={`${jersey20.className} text-2xl ml-3 mt-3`}>Quality Check</h2>
        </div>
    );
}